{% import '_includes/forms' as forms %}

<h2>{{ 'Sidrena cijena (NN 101/2026)'|t('craft-product-price-history') }}</h2>

{{ forms.dateField({
    label: 'Reference day'|t('craft-product-price-history'),
    instructions: 'The anchor price is the price applicable on this day. The decision sets 10 September 2026.'|t('craft-product-price-history'),
    id: 'anchorDate',
    name: 'anchorDate',
    value: settings.anchorDate ? date(settings.anchorDate) : null,
    errors: settings.getErrors('anchorDate'),
}) }}

{{ forms.textField({
    label: 'Label'|t('craft-product-price-history'),
    instructions: 'Printed next to the selling price. "Sidrena cijena" and "dodatna cijena" are both official.'|t('craft-product-price-history'),
    id: 'anchorLabel',
    name: 'anchorLabel',
    value: settings.anchorLabel,
    errors: settings.getErrors('anchorLabel'),
}) }}

{{ forms.lightswitchField({
    label: 'Products listed after the reference day use their first price'|t('craft-product-price-history'),
    instructions: 'The anchor then carries the first listing date instead of the reference day.'|t('craft-product-price-history'),
    id: 'anchorFromFirstListing',
    name: 'anchorFromFirstListing',
    on: settings.anchorFromFirstListing,
}) }}

{{ forms.textField({
    label: 'Lowest-price window (days)'|t('craft-product-price-history'),
    instructions: 'Days before a reduction whose lowest price must be shown (Zakon o zaštiti potrošača, čl. 19).'|t('craft-product-price-history'),
    id: 'lowestPriceDays',
    name: 'lowestPriceDays',
    type: 'number',
    size: 4,
    value: settings.lowestPriceDays,
    errors: settings.getErrors('lowestPriceDays'),
}) }}

<hr>

<h2>{{ 'Cjenik (price list files)'|t('craft-product-price-history') }}</h2>

{{ forms.lightswitchField({
    label: 'Publish the daily price list'|t('craft-product-price-history'),
    instructions: 'Run `craft craft-product-price-history/price-list/publish` from cron every day before 08:00.'|t('craft-product-price-history'),
    id: 'priceListEnabled',
    name: 'priceListEnabled',
    on: settings.priceListEnabled,
}) }}

{{ forms.textField({
    label: 'Oblik prodajnog objekta'|t('craft-product-price-history'),
    instructions: 'Part of the file name (čl. VI), e.g. "internetska trgovina".'|t('craft-product-price-history'),
    id: 'storeType', name: 'storeType', value: settings.storeType, errors: settings.getErrors('storeType'),
}) }}

{{ forms.textField({
    label: 'Adresa prodajnog objekta'|t('craft-product-price-history'),
    id: 'storeAddress', name: 'storeAddress', value: settings.storeAddress, errors: settings.getErrors('storeAddress'),
}) }}

{{ forms.textField({
    label: 'Oznaka prodajnog objekta'|t('craft-product-price-history'),
    id: 'storeCode', name: 'storeCode', value: settings.storeCode, errors: settings.getErrors('storeCode'),
}) }}

{{ forms.textField({
    label: 'Publish directory'|t('craft-product-price-history'),
    instructions: 'Under the web root so the files are public; aliases allowed.'|t('craft-product-price-history'),
    id: 'priceListPath', name: 'priceListPath', value: settings.priceListPath, errors: settings.getErrors('priceListPath'),
}) }}

{{ forms.textField({
    label: 'Index route'|t('craft-product-price-history'),
    instructions: 'Public page that lists the published files.'|t('craft-product-price-history'),
    id: 'priceListRoute', name: 'priceListRoute', value: settings.priceListRoute, errors: settings.getErrors('priceListRoute'),
}) }}

{{ forms.textField({
    label: 'Retention (days)'|t('craft-product-price-history'),
    instructions: 'Files stay published this long (čl. VII: 30).'|t('craft-product-price-history'),
    id: 'retentionDays', name: 'retentionDays', type: 'number', size: 4, value: settings.retentionDays, errors: settings.getErrors('retentionDays'),
}) }}

{{ forms.textField({
    label: 'CSV delimiter'|t('craft-product-price-history'),
    id: 'csvDelimiter', name: 'csvDelimiter', size: 2, value: settings.csvDelimiter, errors: settings.getErrors('csvDelimiter'),
}) }}

<h3>{{ 'Field mapping'|t('craft-product-price-history') }}</h3>
<p class="light">{{ 'Handles of the fields that feed the optional columns. Leave empty to publish the column blank.'|t('craft-product-price-history') }}</p>

{{ forms.textField({ label: 'Marka (brand)'|t('craft-product-price-history'), id: 'brandField', name: 'brandField', value: settings.brandField }) }}
{{ forms.textField({ label: 'Barkod'|t('craft-product-price-history'), id: 'barcodeField', name: 'barcodeField', value: settings.barcodeField }) }}
{{ forms.textField({ label: 'Jedinica mjere'|t('craft-product-price-history'), id: 'unitField', name: 'unitField', value: settings.unitField }) }}
{{ forms.textField({ label: 'Cijena za jedinicu mjere'|t('craft-product-price-history'), id: 'unitPriceField', name: 'unitPriceField', value: settings.unitPriceField }) }}
